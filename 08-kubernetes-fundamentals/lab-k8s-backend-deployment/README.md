# Lab: Kubernetes Backend Deployment

Deploy TaskFlow API to Minikube — Deployment with replicas, ClusterIP Service, probes, scale, and rolling updates.

```bash
./scripts/solve.sh && ./scripts/check.sh
kubectl apply -f deliverables/
```