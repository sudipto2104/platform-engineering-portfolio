# Lab: ConfigMaps, Secrets & Stateful Data

Externalize TaskFlow config, store credentials in Secrets, run PostgreSQL as StatefulSet with PVC, Redis as Deployment.

```bash
./scripts/solve.sh && ./scripts/check.sh
```