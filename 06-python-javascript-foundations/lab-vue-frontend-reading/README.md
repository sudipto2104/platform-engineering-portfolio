# Lab: Vue 3 Frontend Code Reading

Explore the TaskFlow Vue 3 frontend — components, Pinia stores, Vue Router, and API integration before containerizing the UI.

## Workspace

Code under `workspace/taskflow-ui/` — read-only comprehension exercise.

```bash
./scripts/solve.sh && ./scripts/check.sh
```