# Vue 3 Reading Drills

Answer by reading `workspace/taskflow-ui/`.

1. Where is the Vue app created and which plugins are registered?
2. Which component renders the task list?
3. How does Pinia store `useTaskStore` fetch tasks?
4. What routes exist in the router config?
5. Where is the API base URL configured?
6. How does `TaskCard.vue` display task status?
7. Which view handles the dashboard layout?

Document answers in `deliverables/FRONTEND_READING_NOTES.md`.