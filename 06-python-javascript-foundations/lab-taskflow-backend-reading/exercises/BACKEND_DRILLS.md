# Backend Reading Drills

Answer by reading `workspace/taskflow-api/`.

1. Where is the Flask app created? What does `create_app()` configure?
2. Which file registers `/api/tasks` routes?
3. What fields does the `Task` model define?
4. How does `TaskService.list_tasks()` fetch data?
5. Which schema validates `POST /api/tasks` input?
6. What port does `run.py` bind when started locally?
7. Where is the database URL configured?

Document answers in `deliverables/BACKEND_READING_NOTES.md`.