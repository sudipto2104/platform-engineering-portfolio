# Lab: TaskFlow Backend Code Reading

Explore the TaskFlow Python backend before Docker — project structure, startup, routes, models, services, and validation schemas.

## Workspace

Code under `workspace/taskflow-api/` — read with terminal tools (`tree`, `grep`, `less`, `head`).

## Exercises

See `exercises/BACKEND_DRILLS.md` for guided reading questions.

```bash
./scripts/solve.sh && ./scripts/check.sh
```