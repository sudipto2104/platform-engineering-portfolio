# Lab: Dockerfile Containerization

Containerize TaskFlow frontend (multi-stage Node → Nginx) and backend (FastAPI + `/health` + Docker HEALTHCHECK).

## Deliverables

- `deliverables/Dockerfile.backend`
- `deliverables/Dockerfile.frontend`
- `deliverables/nginx.conf`

```bash
./scripts/solve.sh && ./scripts/check.sh
```