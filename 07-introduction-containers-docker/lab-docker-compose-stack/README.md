# Lab: Docker Compose Stack

Orchestrate TaskFlow frontend, backend, PostgreSQL, and Redis with named volumes, health checks, and backup/restore scripts.

```bash
./scripts/solve.sh && ./scripts/check.sh
```