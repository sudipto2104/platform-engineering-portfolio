# Lab: Docker Registry & Image Distribution

Tag TaskFlow images with semantic versions and practice push/pull against a **simulated** local registry (no Docker Hub account required).

```bash
./scripts/solve.sh && ./scripts/check.sh
```