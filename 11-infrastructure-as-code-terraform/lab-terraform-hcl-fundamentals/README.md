# Lab: Terraform HCL Fundamentals

Learn `terraform init`, `plan`, `apply`, variables, outputs, and validation with TaskFlow starter AWS resources.

```bash
./scripts/solve.sh && ./scripts/check.sh
cd deliverables/terraform && terraform init && terraform validate
```