# Lab: VPC & IAM Security Architecture

Production TaskFlow VPC — public/private subnets, IGW, NAT, security groups, IAM roles with least privilege.

```bash
./scripts/solve.sh && ./scripts/check.sh
cd deliverables/terraform && terraform validate
```