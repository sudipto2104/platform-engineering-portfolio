# Lab: Loops & Batch Automation

for/while/until loops, arrays, nested iteration — deploy servers, process logs, validate configs, rotate backups.

```bash
./scripts/solve.sh && ./scripts/check.sh
```