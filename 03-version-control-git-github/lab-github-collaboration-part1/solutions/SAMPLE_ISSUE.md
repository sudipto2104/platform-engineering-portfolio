# Sample Issue #12 — Task count documentation

**Labels:** enhancement, documentation, taskflow

## User story

As an **API consumer**, I want **task count documented**, so that **I can rely on pagination metadata**.

## Acceptance criteria

- [ ] `docs/API.md` describes `count` field
- [ ] Example `curl` output included
- [ ] Linked PR closes this issue

## Technical notes

`GET /api/tasks` already returns `count` — documentation only.