# Lab: Cloud Network Architecture Design

Design TaskFlow VPCs for dev, staging, and production — **planning only** (AWS implementation Week 10).

Deliverables: YAML network specs + architecture rationale (no cloud resources created).

```bash
./scripts/solve.sh && ./scripts/check.sh
```