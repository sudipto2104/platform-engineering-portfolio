# Lab: Load Balancing & Reverse Proxy

Nginx load balancing across multiple TaskFlow backends — round-robin, least_conn, ip_hash, health checks, sticky sessions.

```bash
./scripts/solve.sh && ./scripts/check.sh
```