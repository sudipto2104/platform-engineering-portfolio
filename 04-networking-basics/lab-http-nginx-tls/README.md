# Lab: HTTP, Nginx & TLS

HTTP methods, status codes, headers, curl testing, Nginx reverse proxy, and self-signed TLS for TaskFlow.

```bash
./scripts/solve.sh && ./scripts/check.sh
```

Requires Docker for live stack (optional — configs verified offline).