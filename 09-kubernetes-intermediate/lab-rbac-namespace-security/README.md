# Lab: RBAC, Namespace Isolation & Security Policies

Multi-tenant TaskFlow — per-environment namespaces, least-privilege RBAC, NetworkPolicy, and pod security contexts.

```bash
./scripts/solve.sh && ./scripts/check.sh
```