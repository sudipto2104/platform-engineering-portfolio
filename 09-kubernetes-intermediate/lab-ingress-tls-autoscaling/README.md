# Lab: Ingress, TLS & Horizontal Pod Autoscaling

Expose TaskFlow via Nginx Ingress Controller 1.12+, terminate TLS, and autoscale API/UI with HPA.

```bash
./scripts/solve.sh && ./scripts/check.sh
```