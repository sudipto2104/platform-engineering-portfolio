# Lab: Linux Security & Access Control

Create a secure multi-user layout for TaskFlow: users, groups, permissions, and a security audit.

## Workspace

Simulated server tree in `workspace/taskflow-server/` — apply permissions without root on your laptop, or use `sudo` on Ubuntu.

## Deliverables

- `deliverables/SECURITY_AUDIT.md` — findings and hardening notes
- `workspace/taskflow-server/` — corrected permission layout

## Verify

```bash
./scripts/check.sh
```

## Solution

```bash
./scripts/solve.sh
```